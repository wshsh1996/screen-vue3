/**
 * 自定义axios request文件路径
 */
const CUSTOM_REQUEST_FILE_PATH = '/@/utils/request'

/**
 * 生成api文件路径 生成目录controller，interface，swagger
 */
const BASE_FILE_PATH = './src/api'

/**
 *  生成目录renderVue
 */
const BASE_FILE_VIEWS_PATH = './'

/**
 * Sawgger文件路径
 */

const Swagger_PATH = 'http://dev.cockpit.jingfang.site/swagger/all/swagger.json'

module.exports = {
    CUSTOM_REQUEST_FILE_PATH,
    BASE_FILE_PATH,
    Swagger_PATH,
    BASE_FILE_VIEWS_PATH
}
